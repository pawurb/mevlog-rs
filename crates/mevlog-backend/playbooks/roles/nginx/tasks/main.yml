- name: Copy website config file
  copy:
    src: mevlog.rs.conf
    dest: /etc/nginx/conf.d/
    owner: root
    mode: '0644'

# Reload, not restart: the box serves other sites from the same nginx.
- name: Reload nginx
  service: name=nginx state=reloaded
