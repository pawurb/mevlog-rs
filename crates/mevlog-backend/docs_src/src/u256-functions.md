# U256 SQL Functions

Custom SQLite functions for U256 BLOB columns: `u256_sum`, `u256_mul`,
`u256_add`, `u256_to_dec`, `erc20_to_real`, and the `format_ether` / `format_gwei`
/ `format_usd` display helpers.
