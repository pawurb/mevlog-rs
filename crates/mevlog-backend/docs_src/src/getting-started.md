# Getting Started

## Install

```bash
cargo install mevlog
```

## First query

Collect transactions across a block range:

```bash
mevlog query -b 100:latest
```

That's the minimal flow. More commands and the SQL query layer are documented in
later sections.
